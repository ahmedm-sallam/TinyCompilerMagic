{ Sample program
  in TINY language
  compute factorial
}

read x; {input an integer}
if 0<x then {compute only if x>=1}
  fact:=1;
  repeat
    fact := fact * x;
    value:= (2 + 3) ^ (2 + 1);
    x:=x-1
  until x=0;
  write fact {output factorial}
end